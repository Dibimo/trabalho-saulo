import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        height: '25%',
        margin: 12,
        borderWidth: 1,
        fontSize: 12,
    },
    listaDeNotas:{
        display: "flex",
        alignItems: "center",
    }
});